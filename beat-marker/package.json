{
  "name": "beat-marker",
  "version": "0.1.0",
  "description": "Beat Marker — Adobe Premiere Pro UXP plugin for automatic beat/BPM detection and real timeline markers.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test/run.mjs",
    "bench": "node bench/benchmark.mjs",
    "build": "esbuild src/ui/panel.js --bundle --format=iife --platform=browser --outfile=dist/panel.js --legal-comments=none"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
